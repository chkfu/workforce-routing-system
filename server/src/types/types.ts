import { types } from 'util';

export type type_dept_row = {
  _id: string;
  dept_name: string;
  dept_capacity: number;
  importance_weight: number;
  is_active: boolean;
  created_at: Date;
  updated_at: Date;
};
